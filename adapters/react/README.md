# React adapter

`@epistemic-protocol/react` contains dependency-light `DecisionBadge` and `CertificateView` components that accept only portable v0.1 fields. They can be embedded in third-party UIs without importing Epistemic Engine models.
