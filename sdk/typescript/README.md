# TypeScript SDK

The v0.1 package exports portable event/decision types, local validation, canonical hashing, and Remote, File, and No-op providers. `npm test` checks its canonical hash against the shared cross-language fixture.
