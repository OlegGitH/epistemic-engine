# Python SDK

The v0.1 package exposes portable dictionary validation, canonical hashing, and Remote, File, and No-op providers without an engine or model-vendor dependency.

```bash
python -m unittest discover -s tests
```
