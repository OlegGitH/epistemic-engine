{
  "claim_id": "claim_current_status_compatibility",
  "claim": "Every currently supported persisted order status remains readable without changing its meaning.",
  "check": "Add one table-driven regression test proving pending, in_progress, and completed each round-trip through migrate_status unchanged.",
  "required_test": "tests/test_codex_status_migration.py"
}
