# Exported schemas

These are language-neutral release snapshots of the canonical contracts under `apps/control-plane/api/schemas/`. Keep both locations byte-for-byte aligned when versioning a schema.
